services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bansal-react-frontend
    ports:
      # Map host port 8085 to container port 80
      - "8085:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
